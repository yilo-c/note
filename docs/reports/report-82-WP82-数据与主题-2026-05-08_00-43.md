# Implementation Report: 82-WP82-数据与主题.md

**Date:** 2026-05-08 01:27
**Status:** DONE
Task: WP82-数据与主题
Status: Done
Files Created:
- src/utils/trash.ts — TrashItem type + CRUD operations on localStorage `desk-notes-trash`
- src/components/Common/TrashPanel.tsx — Modal panel for viewing/restoring/permanently deleting trashed items
Files Modified:
- src/types/index.ts — Added `archived?: boolean` and `tags?: string[]` to FloatingNote
- src/store/useStore.ts — Integrated trash (deleteTodo/removeFloatingNote → moveToTrash), added `theme`, `sortMode` state, `toggleTheme()`, `setSortMode()`, `permanentlyDeleteTodo()`, `permanentlyDeleteNote()`
- src/index.css — Defined CSS variables for dark/light themes, `:root` + `[data-theme="light"]` selectors, 0.3s transition
- src/main.tsx — Restore persisted theme before render to prevent flash
- src/components/AcrylicPanel/AcrylicPanel.tsx — Added trash button (fa-trash-can), theme toggle button (fa-sun/fa-moon), TrashPanel modal
- src/components/FloatingNote/FloatingNote.tsx — Archive button (fa-box-archive), tag editor popup (fa-tags), colored dot tags, archived banner, archived notes can't drag/resize, context menu archive option
- src/components/FloatingNote/NotesLayer.tsx — "查看归档" toggle switch, tag filter dropdown, tag search matching
- src/components/Todo/TodoList.tsx — Sort button cycling through created/priority/dueDate/alpha, sort functions for each mode, completed items always after active
Libraries Added: None
Test Results: TypeScript type check passed (0 errors), Vite build successful (415 modules, 6.36s)
Notes:
- Theme uses CSS custom properties on `:root` (dark) and `[data-theme="light"]` (light). Most existing inline rgba values remain dark-specific; the theme system applies CSS variable-based overrides to the main surfaces (panel backgrounds, text, borders) via `var(--panel-bg)` etc. in React inline styles where practical.
- Tag colors are auto-assigned via string hash from tag name, giving consistent colors per tag.
- Trash uses a separate localStorage key `desk-notes-trash` to avoid polluting the main store's persist data with deleted items.
- Archived notes in the main view are hidden by default; switching to "查看归档" mode shows only archived notes, semi-transparent with a banner.

---
*Generated by run-overnight.ps1*
