# 思忆便签 — 使用说明

---

## 一、界面总览

思忆便签 = 轻量级浮动便签 + 待办管理。主面板分为两个模式：**待办模式**和**笔记模式**。

```
┌──────────────────────────────────────────────────┐
│  [🔒] [🗑] [📌] [🔒面板] [◐] [-] [✕]           │ ← 标题栏
│  思忆便签    [✓ 待办] [📝 笔记]                   │ ← 模式切换
├──────────────────────────────────────────────────┤
│  📅 今日概览（日期 + 农历 + 宜忌）                │ ← 晨间简报
├──────────────────────────────────────────────────┤
│  🔍 搜索...  [📅日历] [⇅排序] [☑批量]           │ ← 工具栏
│  [全部] [进行中] [已完成] [已逾期] | P0 P1 P2    │ ← 筛选栏
├──────────────────────────────────────────────────┤
│  个人 · 重要 · 读书 · 日程 · ...                  │ ← 分类栏
├──────────────────────────────────────────────────┤
│  ○ 完成项目演示 PPT        14:30  🔴             │ ← 待办项
│  ✓ 整理本周工作周报         昨日                  │
│  ──── 2 项已完成 ──── [清除] ────                │ ← 统计栏
├──────────────────────────────────────────────────┤
│  [+ 新建文本]   [✓ 新建待办]   [📁 文件夹]       │ ← 底部工具栏
└──────────────────────────────────────────────────┘
```

---

## 二、安装与启动

### Windows 安装

1. 下载 `思忆便签 Setup x.x.x.exe` 安装包，双击运行
2. 安装完成后，桌面和开始菜单出现「思忆便签」图标
3. 首次启动后，应用会在任务栏显示 ∞ 图标

### 便携版

- 下载 `思忆便签-便携版-x.x.x.exe`，双击直接运行，无需安装
- 数据保存在可执行文件同目录下

---

## 三、待办模式

### 晨间简报

面板顶部显示当日日期、农历、黄历宜忌概要，帮助规划一天事项。

### 搜索与筛选

| 功能 | 操作 |
|------|------|
| 关键词搜索 | 在搜索框输入文字，实时过滤待办 |
| 清空搜索 | 点击搜索框右侧 ✕ |
| 状态筛选 | 点击「全部 / 进行中 / 已完成 / 已逾期」切换 |
| 优先级筛选 | 点击 P0 / P1 / P2 筛选对应优先级 |

### 排序

点击 ⇅ 排序按钮弹出菜单，可选：
- **创建时间** — 按添加顺序排列
- **优先级** — P0 > P1 > P2
- **截止日期** — 按到期时间排列
- **字母顺序** — 按内容文字排序

再次点击当前排序方式可切换升序/降序。

### 视图切换

点击 📅 按钮在列表视图和日历视图之间切换。

### 添加待办

- 点击底部工具栏「✓ 新建待办」，出现输入框
- 输入内容后按 `Enter` 添加
- 可设置优先级、截止日期、子任务

### 待办操作

| 操作 | 说明 |
|------|------|
| 点击空心圆 | 标记为已完成 |
| 点击勾选圆 | 恢复为未完成 |
| 按住待办拖出 | 创建独立浮动窗口 |
| 右键待办 | 弹出上下文菜单（编辑/删除/优先级等） |
| 点击优先级圆点 | 循环切换 P0 → P1 → P2 → 无 |

### 批量操作

点击 ☑ 按钮进入批量模式：
- 勾选多项待办后，可批量完成、删除或移动
- 再次点击 ☑ 退出批量模式

---

## 四、笔记模式

点击标题栏「📝 笔记」切换到笔记模式。

### 文件夹管理

左侧显示文件夹树：
- 点击文件夹筛选笔记
- 支持多级文件夹嵌套
- 展开/折叠子文件夹

### 笔记列表

| 功能 | 操作 |
|------|------|
| 搜索笔记 | 搜索框输入关键词 |
| 标签筛选 | 点击标签名筛选，再次点击取消 |
| 排序 | 按创建时间 / 更新时间 / 字母顺序 |
| 查看归档 | 点击 📦 归档按钮 |
| 批量操作 | 点击 ☑ 进入批量模式 |

### 新建笔记

底部工具栏 →「+ 新建文本」创建文本便签。

### 笔记操作

| 操作 | 说明 |
|------|------|
| 点击笔记 | 进入编辑模式 |
| 按住拖出 | 创建独立浮动窗口 |
| 右键笔记 | 弹出菜单（归档/移动/删除） |

---

## 五、浮动窗口

浮动窗口是独立的便签窗口，可脱离主面板自由放置。

### 标题栏操作

| 按钮 | 功能 |
|------|------|
| 📌 | 置顶窗口，始终在最前 |
| 🔒 | 锁定窗口位置，不可拖动 |
| ◐ | 调节该窗口的透明度 |
| ✕ | 关闭窗口 |

### 拖动与缩放

- 拖动标题栏移动窗口
- 窗口右下角拖拽手柄可缩放

---

## 六、外观设置

点击标题栏 ◐ 按钮打开外观面板：

| 设置 | 选项 |
|------|------|
| 主题 | 深色 / 浅色 |
| 背景模式 | 亚克力毛玻璃 / 纯白 / 纯黑 |
| 透明度 | 滑块 15% ~ 100% |
| 色调 | 预设颜色圆点或自定义取色 |
| 语言 | 中文 / English |

---

## 七、数据存储

- Windows 版数据保存在 `%APPDATA%\思忆便签\` 目录
- 包含待办、笔记内容、设置等，卸载时不会自动删除
- 便携版数据保存在可执行文件同目录下

---

## 八、键盘快捷键

| 快捷键 | 功能 |
|--------|------|
| Enter | 确认输入 |
| Esc | 关闭应用（桌面版） |

---

## 九、常见操作速查

| 我想... | 怎么做 |
|---------|--------|
| 记一个待办 | 待办模式 → 底部「✓ 新建待办」→ 输入 → Enter |
| 写一段备忘 | 笔记模式 → 底部「+ 新建文本」 |
| 完成一项任务 | 点击待办左侧空心圆 ○ |
| 看日历视图 | 待办模式 → 点击 📅 按钮 |
| 只看高优先级 | 筛选栏点击 P0 |
| 让面板置顶 | 标题栏点击 📌 |
| 锁定面板位置 | 标题栏点击 🔒 |
| 调透明度 | 标题栏 ◐ → 拖动滑块 |
| 切换中英文 | 标题栏 ◐ → 语言设置 |
| 把待办拖成独立窗口 | 按住待办项 → 拖出面板 |
| 恢复已删除内容 | 标题栏 🗑 → 回收站 |
